name: Tests

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: tests-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Python 3.12
    runs-on: ubuntu-latest
    timeout-minutes: 20

    defaults:
      run:
        shell: bash -leo pipefail {0}

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Create minimal test environment
        uses: mamba-org/setup-micromamba@v3
        with:
          condarc: |
            channels:
              - conda-forge
              - bioconda
            channel_priority: strict
          environment-name: skype-tests
          cache-environment: true
          cache-downloads: true
          # Avoid restoring an x86-64-v4 cache on x86-64-v3 runners.
          create-args: >-
            python=3.12
            numpy
            pandas
            networkx
            tqdm
            graph-tool
            vcfpy
            _x86_64-microarch-level=3

      - name: Run test suite
        run: python -m unittest discover -s tests -v
